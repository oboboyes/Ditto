<Page
    x:Class="DittoWinUI.Views.QuickPasteWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DittoWinUI.Views"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    Title="Quick Paste">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Light">
                    <SolidColorBrush x:Key="AccentBrush" Color="{ThemeResource SystemAccentColor}"/>
                </ResourceDictionary>
                <ResourceDictionary x:Key="Dark">
                    <SolidColorBrush x:Key="AccentBrush" Color="{ThemeResource SystemAccentColor}"/>
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>

            <!-- Fade in animation -->
            <Storyboard x:Name="FadeInStoryboard">
                <DoubleAnimation
                    Storyboard.TargetName="RootGrid"
                    Storyboard.TargetProperty="Opacity"
                    From="0.0" To="1.0" Duration="0:0:0.2">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut"/>
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
                <DoubleAnimation
                    Storyboard.TargetName="RootGrid"
                    Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                    From="20" To="0" Duration="0:0:0.25">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut"/>
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
            </Storyboard>

            <!-- Fade out animation -->
            <Storyboard x:Name="FadeOutStoryboard">
                <DoubleAnimation
                    Storyboard.TargetName="RootGrid"
                    Storyboard.TargetProperty="Opacity"
                    From="1.0" To="0.0" Duration="0:0:0.15">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseIn"/>
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
            </Storyboard>

            <!-- List item entrance animation -->
            <Storyboard x:Name="ListItemEntrance">
                <DoubleAnimation
                    Storyboard.TargetProperty="Opacity"
                    From="0" To="1" Duration="0:0:0.2">
                    <DoubleAnimation.EasingFunction>
                        <QuadraticEase EasingMode="EaseOut"/>
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
                <DoubleAnimation
                    Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                    From="-20" To="0" Duration="0:0:0.25">
                    <DoubleAnimation.EasingFunction>
                        <QuadraticEase EasingMode="EaseOut"/>
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
            </Storyboard>

            <!-- Loading spinner animation -->
            <Storyboard x:Name="LoadingStoryboard" RepeatBehavior="Forever">
                <DoubleAnimation
                    Storyboard.TargetName="LoadingProgress"
                    Storyboard.TargetProperty="Rotation"
                    From="0" To="360" Duration="0:0:1">
                    <DoubleAnimation.EasingFunction>
                        <LinearEase/>
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
            </Storyboard>

            <!-- Hover animation for list items -->
            <Storyboard x:Name="HoverEnterStoryboard">
                <DoubleAnimation
                    Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                    To="1.02" Duration="0:0:0.1">
                    <DoubleAnimation.EasingFunction>
                        <QuadraticEase EasingMode="EaseOut"/>
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
                <DoubleAnimation
                    Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                    To="1.02" Duration="0:0:0.1">
                    <DoubleAnimation.EasingFunction>
                        <QuadraticEase EasingMode="EaseOut"/>
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
                <ColorAnimation
                    Storyboard.TargetProperty="(Control.Background).(SolidColorBrush.Color)"
                    To="#20000000" Duration="0:0:0.1"/>
            </Storyboard>

            <Storyboard x:Name="HoverExitStoryboard">
                <DoubleAnimation
                    Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                    To="1.0" Duration="0:0:0.1">
                    <DoubleAnimation.EasingFunction>
                        <QuadraticEase EasingMode="EaseOut"/>
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
                <DoubleAnimation
                    Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                    To="1.0" Duration="0:0:0.1">
                    <DoubleAnimation.EasingFunction>
                        <QuadraticEase EasingMode="EaseOut"/>
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
            </Storyboard>
        </ResourceDictionary>
    </Page.Resources>

    <Grid x:Name="RootGrid" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RenderTransform>
            <TranslateTransform/>
        </Grid.RenderTransform>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="16,12,16,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <AutoSuggestBox Grid.Column="0" x:Name="SearchBox"
                PlaceholderText="Type to search..."
                TextChanged="SearchBox_TextChanged"
                QuerySubmitted="SearchBox_QuerySubmitted">
                <AutoSuggestBox.Triggers>
                    <EventTrigger RoutedEvent="AutoSuggestBox.TextChanged">
                        <BeginStoryboard Storyboard="{StaticResource FadeInStoryboard}"/>
                    </EventTrigger>
                </AutoSuggestBox.Triggers>
            </AutoSuggestBox>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="16,0,0,0">
                <Button x:Name="SettingsButton" Content="&#xE713;" FontFamily="Segoe MDL2 Assets" 
                    ToolTipService.ToolTip="Settings" Margin="4,0">
                    <Button.Resources>
                        <ResourceDictionary>
                            <ResourceDictionary.ThemeDictionaries>
                                <ResourceDictionary x:Key="Light">
                                    <SolidColorBrush x:Key="ButtonBackground" Color="Transparent"/>
                                </ResourceDictionary>
                                <ResourceDictionary x:Key="Dark">
                                    <SolidColorBrush x:Key="ButtonBackground" Color="Transparent"/>
                                </ResourceDictionary>
                            </ResourceDictionary.ThemeDictionaries>
                        </ResourceDictionary>
                    </Button.Resources>
                    <Button.Transitions>
                        <TransitionCollection>
                            <RevealThemeTransition IsStaggeringEnabled="True"/>
                        </TransitionCollection>
                    </Button.Transitions>
                </Button>
                <Button x:Name="CloseButton" Content="&#xE8BB;" FontFamily="Segoe MDL2 Assets" 
                    ToolTipService.ToolTip="Close" Click="CloseButton_Click" Margin="4,0"/>
            </StackPanel>
        </Grid>

        <ListView Grid.Row="1" x:Name="ClipListView"
            SelectionMode="Single"
            SelectionChanged="ClipListView_SelectionChanged"
            IsItemClickEnabled="True"
            ItemClick="ClipListView_ItemClick"
            Margin="8,0,8,8"
            IsSynchronizedWithCurrentItem="True"
            CanReorderItems="False"
            AllowDrop="False"
            VirtualizingPanel.IsVirtualizing="True"
            VirtualizingPanel.VirtualizationMode="Recycling">
            <ListView.Transitions>
                <TransitionCollection>
                    <AddDeleteThemeTransition/>
                    <ReorderThemeTransition IsStaggeringEnabled="True"/>
                </TransitionCollection>
            </ListView.Transitions>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="12,8" Margin="0,2">
                        <Grid.RenderTransform>
                            <ScaleTransform ScaleX="1" ScaleY="1"/>
                        </Grid.RenderTransform>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Column="0" Grid.Row="0"
                            Text="{Binding Description}"
                            TextTrimming="CharacterEllipsis"
                            FontSize="14"
                            FontWeight="SemiBold"/>

                        <StackPanel Grid.Column="1" Grid.RowSpan="2" 
                            Orientation="Horizontal" 
                            VerticalAlignment="Center">
                            <SymbolIcon Symbol="Pin" Visibility="{Binding IsPinned}" Margin="8,0"/>
                            <TextBlock Text="{Binding GroupId}" 
                                FontSize="12" 
                                Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                Margin="8,0"/>
                        </StackPanel>

                        <TextBlock Grid.Column="0" Grid.Row="1"
                            Text="{Binding Created}"
                            FontSize="12"
                            Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                            Margin="0,4,0,0"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="4,2"/>
                    <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <ScaleTransform ScaleX="1" ScaleY="1"/>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Transitions">
                        <Setter.Value>
                            <TransitionCollection>
                                <PointerDownThemeTransition/>
                            </TransitionCollection>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <Grid Grid.Row="1" x:Name="EmptyState" Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <SymbolIcon Symbol="Clipboard" FontSize="64" 
                    Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}">
                    <SymbolIcon.Transitions>
                        <TransitionCollection>
                            <FadeInThemeTransition/>
                        </TransitionCollection>
                    </SymbolIcon.Transitions>
                </SymbolIcon>
                <TextBlock Text="No clips found" 
                    FontSize="18" 
                    Margin="0,16,0,0"
                    HorizontalAlignment="Center">
                    <TextBlock.Transitions>
                        <TransitionCollection>
                            <FadeInThemeTransition/>
                        </TransitionCollection>
                    </TextBlock.Transitions>
                </TextBlock>
                <TextBlock Text="Copy something to get started"
                    FontSize="14"
                    Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                    HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" x:Name="LoadingState" Visibility="Collapsed">
            <ProgressBar x:Name="LoadingProgress" IsIndeterminate="True" 
                Width="200" 
                HorizontalAlignment="Center"
                VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Page>
